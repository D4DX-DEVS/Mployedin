// Shared product constants.

// An application counts as a "high match" when its AI match score is at or
// above this threshold. Keep dashboards, list pages, and aggregations in sync.
export const AI_MATCH_HIGH_THRESHOLD = 80;
